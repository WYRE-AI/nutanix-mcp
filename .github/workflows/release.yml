name: Release

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Thin caller for the canonical reusable release pipeline. The build →
# semantic-release → Docker → MCP Registry → security chain (tag-based release
# detection + correct gating) lives in WYRE-AI/.github so every *-mcp
# repo stays in lockstep instead of drifting per-repo. Caller jobs grant the
# token scopes the reusable jobs request (a reusable can only reduce, never
# expand, caller-granted scope).
jobs:
  release:
    permissions:
      contents: write
      issues: write
      pull-requests: write  # @semantic-release/github success step reads/comments on PRs
      packages: write
      id-token: write
      security-events: write
    uses: WYRE-AI/.github/.github/workflows/mcp-server-release.yml@8c2360058e882a8afacfc4cebf840135789ef406
    with:
      server-name: nutanix-mcp
      image-name: ghcr.io/wyre-ai/nutanix-mcp
    secrets: inherit

# NO `deploy:` job — deliberate. Nutanix is a CONDUIT-ONLY vendor.
#
# The fleet-standard `mcp-server-deploy.yml` deploys to `gwp-<slug>` in the
# `mcp-gateway-prod` resource group — the separate legacy fleet, NOT conduit.
# Conduit's sidecars are deployed from conduit's own vendor-fleet pipeline,
# where nutanix is digest-pinned. There is no `gwp-nutanix` Container App and
# no federated credential registered for this repo, so a deploy job here
# could only ever fail (permanently red leg). This matches the other
# conduit-only vendors: connectwise-cpq-mcp, scalepad-mcp, clio-mcp.
